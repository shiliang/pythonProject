apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "job-service.fullname" . }}-test-connection"
  labels:
    {{- include "job-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "job-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
